apiVersion: v1
kind: Service
metadata:
  name: huginn-threaded
  labels:
    app: {{ template "fullname" . }}-threaded
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
spec:
  clusterIP: None
  ports:
  - name: headless
    port: 55555
    targetPort: 0
  selector:
    component: {{ template "fullname" . }}-threaded
